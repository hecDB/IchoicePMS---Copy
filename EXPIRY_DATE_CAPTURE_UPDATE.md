# 📋 ปรับปรุงการบันทึกวันหมดอายุสินค้า (Expiry Date Capture)

## 📝 สรุปการเปลี่ยนแปลง

ได้ทำการปรับปรุงระบบให้บันทึกวันหมดอายุสินค้า (`expiry_date`) ในตาราง `returned_items` อย่างครบถ้วน ทั้งจากการตีกลับทั่วไป และการตีกลับสินค้าชำรุดบางส่วน

---

## ✅ การปรับปรุงที่ทำ

### 1️⃣ เพิ่มคอลัมน์ `expiry_date` ในตาราง `issue_items`
**ไฟล์:** `api/returned_items_api.php`

- เพิ่มฟังก์ชัน `ensureIssueItemsExpiryColumn()` เพื่อตรวจสอบและเพิ่มคอลัมน์ `expiry_date` ในตาราง `issue_items`
- ฟังก์ชันจะทำงานอัตโนมัติเมื่อเปิดไฟล์ API

```php
function ensureIssueItemsExpiryColumn(PDO $pdo): void
{
    try {
        // Check if expiry_date column exists in issue_items
        $columnsStmt = $pdo->prepare("SELECT COLUMN_NAME FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_SCHEMA = DATABASE() AND TABLE_NAME = 'issue_items' AND COLUMN_NAME = 'expiry_date'");
        $columnsStmt->execute();
        
        if ($columnsStmt->rowCount() === 0) {
            // Add expiry_date column if it doesn't exist
            $pdo->exec("ALTER TABLE issue_items ADD COLUMN expiry_date DATE NULL AFTER issue_qty");
        }
    } catch (Exception $e) {
        error_log('Failed to ensure issue_items expiry_date column: ' . $e->getMessage());
    }
}
```

---

### 2️⃣ ปรับปรุงการบันทึกวันหมดอายุในการตีกลับสินค้า
**ไฟล์:** `api/returned_items_api.php`

#### 📌 การทำงาน:
- ขณะสร้างการตีกลับ (`create_return`)
- ระบบจะดึงข้อมูล `expiry_date` จากตาราง source:
  - **sales order items** (`issue_items`) 
  - **purchase order items** (`receive_items`)
- บันทึกลง `returned_items.expiry_date`

#### 💾 ตัวอย่างการบันทึก:
```php
':expiry_date' => $order_item['expiry_date'] ?? null,
```

---

### 3️⃣ บันทึกวันหมดอายุในการตรวจสอบสินค้าชำรุด
**ไฟล์:** `api/returned_items_api.php`

- ปรับปรุงการบันทึกลงตาราง `damaged_return_inspections` เพื่อให้รวมคอลัมน์ `expiry_date`

**ก่อนปรับปรุง:**
```php
INSERT INTO damaged_return_inspections (
    return_id, return_code, product_id, product_name, sku, barcode, po_id, po_number, return_qty,
    reason_id, reason_name, status, new_sku, defect_notes, created_by, cost_price, sale_price
) VALUES (...)
```

**หลังปรับปรุง:**
```php
INSERT INTO damaged_return_inspections (
    return_id, return_code, product_id, product_name, sku, barcode, expiry_date, po_id, po_number, return_qty,
    reason_id, reason_name, status, new_sku, defect_notes, created_by, cost_price, sale_price
) VALUES (...)
```

---

### 4️⃣ แสดงวันหมดอายุในหน้าแดชบอร์ด
**ไฟล์:** `returns/return_dashboard.php`

- เพิ่มการแสดงวันหมดอายุในหน้ารายละเอียดการตีกลับ (detail modal)
- ใช้รูปแบบวันที่ไทย (Thai locale)

```javascript
${item.expiry_date ? `
<div class="detail-row">
    <div class="detail-label">วันหมดอายุ</div>
    <div class="detail-value">
        <strong>${new Date(item.expiry_date).toLocaleDateString('th-TH')}</strong>
    </div>
</div>
` : ''}
```

---

## 📊 โครงสร้างข้อมูล

### ตารางที่เกี่ยวข้อง:

| ตาราง | คอลัมน์ | ประเภท | รายละเอียด |
|-------|--------|---------|-----------|
| **returned_items** | expiry_date | DATE | วันหมดอายุจากการตีกลับ |
| **damaged_return_inspections** | expiry_date | DATE | วันหมดอายุสำหรับการตรวจสอบชำรุด |
| **issue_items** | expiry_date | DATE | *เพิ่มใหม่* - วันหมดอายุของสินค้า |
| **receive_items** | expiry_date | DATE | *มีอยู่แล้ว* - วันหมดอายุที่รับสินค้า |

---

## 🔄 ขั้นตอนการบันทึกข้อมูล

### 1. การตีกลับจาก Sales Order
```
issue_items (expiry_date) → returned_items (expiry_date)
                         → damaged_return_inspections (expiry_date) [ถ้าชำรุด]
```

### 2. การตีกลับจาก Purchase Order
```
receive_items (expiry_date) → returned_items (expiry_date)
                           → damaged_return_inspections (expiry_date) [ถ้าชำรุด]
```

---

## 🧪 การทดสอบ

### สร้างไฟล์ทดสอบ:
- ไฟล์ `test_expiry_date_capture.php`

### ตรวจสอบได้:
1. ตรวจสอบว่าคอลัมน์ `expiry_date` มีอยู่ในทั้ง 4 ตาราง
2. ตรวจสอบว่า `returned_items` มีข้อมูล `expiry_date`
3. ตรวจสอบว่า `damaged_return_inspections` มีข้อมูล `expiry_date`

---

## 📁 ไฟล์ที่แก้ไข

1. **api/returned_items_api.php**
   - เพิ่มฟังก์ชัน `ensureIssueItemsExpiryColumn()`
   - ปรับปรุง INSERT statement สำหรับ `damaged_return_inspections`

2. **returns/return_dashboard.php**
   - เพิ่มการแสดง `expiry_date` ในหน้ารายละเอียด

3. **test_expiry_date_capture.php** (สร้างใหม่)
   - ไฟล์ทดสอบสำหรับตรวจสอบการบันทึก

---

## 🎯 ผลลัพธ์

✅ ระบบจะบันทึกวันหมดอายุสินค้าใน `returned_items.expiry_date` อย่างครบถ้วน

✅ ข้อมูลวันหมดอายุสามารถแสดงในหน้าแดชบอร์ด

✅ การตรวจสอบสินค้าชำรุดยังบันทึกข้อมูลวันหมดอายุต่อไป

✅ สามารถติดตามสถานะวันหมดอายุของสินค้าตีกลับทั้งหมด

---

## 📞 หมายเหตุ

- ถ้าข้อมูลเดิมไม่มี `expiry_date` จะบันทึกเป็น `NULL`
- ระบบจะแสดงเฉพาะเมื่อมีข้อมูล `expiry_date` ที่ไม่ว่าง
- การแสดงวันที่ใช้รูปแบบไทย (Thai locale)
