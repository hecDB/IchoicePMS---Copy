# ระบบสร้าง PO สำหรับสินค้าใหม่ - สรุปการปรับปรุง

## 📅 วันที่อัพเดต: 16 พฤศจิกายน 2568

## ✨ ฟีเจอร์ที่เพิ่มเข้ามา

### 1. 🔢 สร้าง PO Number อัตโนมัติ
- **ฟีเจอร์:** ปุ่มสร้าง PO Number อัตโนมัติ
- **รูปแบบ:** PO-YYYY-NNNNN (เช่น PO-2025-00001)
- **ไฟล์ API:** `api/generate_po_number_api.php` (ใหม่)
- **ลอจิก:** ค้นหา PO Number สูงสุดในปีปัจจุบันแล้วเพิ่ม 1

### 2. 📸 อัพโหลดรูปภาพสินค้า
- **ฟีเจอร์:** อัพโหลดรูปภาพสำหรับแต่ละรายการสินค้า
- **รูปแบบรองรับ:** JPEG, PNG, GIF, WebP
- **ขนาดสูงสุด:** 5 MB
- **การจัดเก็บ:** Base64 encoded ในคอลัมน์ `product_image`
- **การแสดง:** 
  - รูปตัวอย่างเล็กน้อย (80x80px) ในแบบฟอร์ม
  - รูปสูงสูงกว่า (60x60px) ในตารางอนุมัติ
  - รูปขนาดใหญ่ (150x150px) ในหน้าต่างป็อปอัป

### 3. 🏷️ เลือกประเภทสินค้า
- **ฟีเจอร์:** Dropdown สำหรับเลือกประเภทสินค้า
- **ประเภทที่มี:** 6 ประเภท
  - อาหารเสริม
  - เครื่องใช้ไฟฟ้า
  - เครื่องสำอาง/ความงาม
  - สำหรับแม่และเด็ก
  - สัตว์เลี้ยง
  - เครื่องใช้ในบ้าน/ออฟฟิศ
- **การจัดเก็บ:** บันทึกในคอลัมน์ `product_category`

## 🗄️ การเปลี่ยนแปลงฐานข้อมูล

### ไฟล์ Migration
**ไฟล์:** `db/add_image_category_to_temp_products.sql`

```sql
ALTER TABLE `temp_products` 
ADD COLUMN `product_category` varchar(100) DEFAULT NULL,
ADD COLUMN `product_image` longblob DEFAULT NULL;
```

### คอลัมน์ใหม่
1. **product_category** - VARCHAR(100) - ประเภทสินค้า
2. **product_image** - LONGBLOB - รูปภาพสินค้า (Base64)

## 📝 ไฟล์ที่แก้ไข/สร้างใหม่

### 📄 Frontend Files

#### 1. `orders/purchase_order_create_new_product.php`
**สิ่งที่แก้ไข:**
- เพิ่ม enctype="multipart/form-data" ในแท็ก form
- เพิ่มฟังก์ชัน `generatePONumber()` เพื่อสร้าง PO Number อัตโนมัติ
- อัพเดต `addItemRow()` เพื่อรวมฟิลด์ภาพและประเภท
- เพิ่มฟังก์ชัน `previewImage()` เพื่อแสดงตัวอย่างภาพ
- เพิ่ม CSS สำหรับคอลัมน์ภาพและเลือกประเภท

**CSS ใหม่:**
```css
.image-upload-wrapper { /* Wrapper สำหรับการอัพโหลด */ }
.image-upload-input { /* Input file ซ่อน */ }
.image-upload-btn { /* ปุ่มอัพโหลดรูปภาพ */ }
.product-image-preview { /* ตัวอย่างรูปภาพ */ }
```

#### 2. `products/convert_temp_to_product.php`
**สิ่งที่แก้ไข:**
- เพิ่มคอลัมน์รูปภาพในตาราง (column แรก)
- เพิ่มคอลัมน์ประเภทสินค้า
- อัพเดต openApproveModal() เพื่อรับและแสดงรูปภาพและประเภท
- เพิ่มการแสดงรูปภาพในหน้าต่างป็อปอัป

### 🔗 Backend API Files

#### 1. `api/generate_po_number_api.php` ✨ (ใหม่)
**ลอจิก:**
- ตรวจสอบ PO Number ที่สูงสุดในปีปัจจุบัน
- สร้าง PO Number ถัดไปในรูปแบบ PO-YYYY-NNNNN
- ตรวจสอบความซ้ำซ้อน
- ส่งกลับ JSON response ที่มี po_number

**ตรวจสอบสิทธิ์:** Admin และ Manager เท่านั้น

#### 2. `api/purchase_order_new_product_api.php`
**สิ่งที่แก้ไข:**
- เพิ่มการจัดการฟิลด์ `category[]` จากแบบฟอร์ม
- เพิ่มการจัดการ `product_image[]` (ไฟล์หลายไฟล์)
- ตรวจสอบประเภท MIME ของรูปภาพ
- ตรวจสอบขนาดรูปภาพ (max 5MB)
- แปลงรูปภาพเป็น Base64
- บันทึก product_category และ product_image ลงฐานข้อมูล

**SQL ที่อัพเดต:**
```sql
INSERT INTO temp_products 
(product_name, product_category, product_image, unit, status, po_id, created_by) 
VALUES (?, ?, ?, ?, ?, ?, ?)
```

## 🎨 UI/UX Changes

### หน้าสร้างใบ PO

**ปรับปรุง:**
- เพิ่มปุ่มสร้าง PO Number ขนาด 16px Material Icons
- เพิ่มสถานะการสร้าง PO Number ("✓ สร้าง PO Number สำเร็จ")
- ขยายกริด item-row จาก 6 ไปเป็น 8 คอลัมน์
- เพิ่มคอลัมน์รูปภาพ (flex: 0 0 80px)
- เพิ่มคอลัมน์ประเภทสินค้า (dropdown)

**Grid Layout:**
```
[Image] [Product Name] [Category] [Unit] [Qty] [Price] [Discount] [Delete]
80px    2fr            1.5fr      1fr    1fr   1fr     1fr        80px
```

### หน้าอนุมัติสินค้า

**ปรับปรุง:**
- เพิ่มคอลัมน์รูปภาพในตาราง (ขนาด 60x60px)
- เพิ่มคอลัมน์ประเภทสินค้า
- ปรับคอลัมน์ให้กว้างขึ้นเพื่อแสดงข้อมูลใหม่
- แสดงรูปภาพในหน้าต่างป็อปอัป (150x150px)
- แสดงประเภทสินค้าในหน้าต่างป็อปอัป

## ✅ ตรวจสอบและทดสอบ

### การตรวจสอบ:
- ✓ ไม่มี Syntax Error
- ✓ ไม่มี Undefined Variable Error
- ✓ ไม่มี Database Connection Error

### Checklist ทดสอบ:
```
[ ] 1. สร้างใบ PO ด้วย PO Number อัตโนมัติ
[ ] 2. สร้างใบ PO ด้วย PO Number ที่กรอกเอง
[ ] 3. อัพโหลดรูปภาพสำหรับสินค้า
[ ] 4. เลือกประเภทสินค้า
[ ] 5. ดูรูปภาพในแบบฟอร์ม
[ ] 6. ดูรูปภาพในตารางอนุมัติ
[ ] 7. เปิดหน้าต่างป็อปอัปและดูรูปภาพ+ประเภท
[ ] 8. อนุมัติสินค้าสำเร็จ
[ ] 9. ตรวจสอบข้อมูลในฐานข้อมูล
```

## 🔒 ความปลอดภัย

### การตรวจสอบอัพโหลด:
- ✓ ตรวจสอบประเภท MIME
- ✓ ตรวจสอบขนาดไฟล์
- ✓ ข้อมูลจะถูก Base64 encode
- ✓ ไม่มีการบันทึกไฟล์บนเซิร์ฟเวอร์

### ตรวจสอบสิทธิ์:
- ✓ เฉพาะ Admin/Manager สามารถสร้าง PO ใหม่
- ✓ เฉพาะ Admin สามารถอนุมัติสินค้า

## 📊 สัญญาณรหัสการตอบสนอง

### Success Response (200):
```json
{
  "success": true,
  "message": "สร้างใบ PO สำเร็จ",
  "po_id": 123,
  "po_number": "PO-2025-00001"
}
```

### Error Response (400):
```json
{
  "success": false,
  "message": "ข้อความข้อผิดพลาด"
}
```

### Access Denied (403):
```json
{
  "success": false,
  "message": "Access Denied"
}
```

## 📖 เอกสารที่สำคัญ

1. **`docs/NEW_PRODUCT_PO_DOCUMENTATION.md`** - คู่มือผู้ใช้งานฉบับสมบูรณ์
2. **`db/add_image_category_to_temp_products.sql`** - SQL Migration
3. **`api/generate_po_number_api.php`** - API สร้าง PO Number

## 🚀 การติดตั้ง

1. **รัน Migration SQL:**
   ```sql
   ALTER TABLE `temp_products` 
   ADD COLUMN `product_category` varchar(100) DEFAULT NULL,
   ADD COLUMN `product_image` longblob DEFAULT NULL;
   ```

2. **ตรวจสอบไฟล์ที่อัพเดต:**
   - ✓ `orders/purchase_order_create_new_product.php`
   - ✓ `api/purchase_order_new_product_api.php`
   - ✓ `api/generate_po_number_api.php` (ใหม่)
   - ✓ `products/convert_temp_to_product.php`

3. **ทดสอบระบบ:**
   - สร้างใบ PO ใหม่
   - อนุมัติสินค้า

## 📝 หมายเหตุเพิ่มเติม

- รูปภาพจัดเก็บเป็น Base64 ทำให้ขนาด DB เพิ่มขึ้นเล็กน้อย
- ประเภทสินค้า 6 ประเภทสามารถแก้ไขได้ในโค้ด
- ต้องสร้างไดเรกทอรี `temp_uploads` หากใช้การจัดเก็บไฟล์ (ปัจจุบันใช้ Base64)

---

**สถานะ:** ✅ เสร็จสิ้นและพร้อมใช้งาน

**เวอร์ชัน:** 1.0

**ผู้พัฒนา:** AI Assistant
