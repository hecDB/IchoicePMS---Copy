# 🔍 ตรวจสอบและแก้ไขปัญหา: Popup รับเข้าสินค้าไม่แสดงรายการสินค้าชำรุด

## ปัญหาที่พบ

**Popup รับเข้าสินค้า (receive_po_items.php) ไม่แสดงรายการสินค้าชำรุด (ส่วน `damagedUnsellableSection`) ในตาราง returned_items**

ส่วนนี้ควรจะแสดงรายการสินค้าที่มี:
- `returned_items.is_returnable = 0` (ไม่สามารถคืนสต็อก)
- และมี `po_id` เท่ากับ PO ที่กำลังรับเข้า

---

## ต้นตอของปัญหา 🎯

### ปัญหาหลัก 1: ขาด reason "สินค้าชำรุดบางส่วน"

ระบบต้องการ reason ที่มี `reason_name = 'สินค้าชำรุดบางส่วน'` แต่:

**ตาราง `return_reasons` ไม่มี reason นี้เลย!**

```sql
-- ดูรายการ reasons ที่มี is_returnable = 0:
SELECT * FROM return_reasons WHERE is_returnable = 0;

-- ผลอาจแสดง:
-- 003: ชำรุด/เสียหาย
-- 006: สินค้าปลอม
-- 007: อื่นๆ

-- ❌ ไม่มี "สินค้าชำรุดบางส่วน"
```

**ผลกระทบ:**
1. ในไฟล์ `receive_po_items.php` ฟังก์ชัน `fetchDamagedReasonId()` จะหา reason นี้ไม่เจอ
2. ตัวแปร `damagedReasonId` จะเป็น `null`
3. พอกดปุ่มชำรุด Modal จะแสดง "ไม่พบเหตุผล สินค้าชำรุดบางส่วน"
4. ข้อมูลสินค้าชำรุดไม่ได้ถูกสร้างขึ้นในตาราง `returned_items`
5. เมื่อเปิด popup รับเข้าสินค้า API `get_damaged_unsellable_by_po.php` ก็ไม่เจอข้อมูล
6. ส่วน `damagedUnsellableSection` จึงไม่แสดง

### ปัญหาหลัก 2: ข้อมูล `po_id` อาจไม่ถูกบันทึก

แม้ว่า API จะรับ `po_id` มา แต่หากกำหนดอื่นผิด ข้อมูลอาจไม่ถูกบันทึก:

```php
// ใน returned_items_api.php line 846
':is_returnable' => $reason['is_returnable'],

// หากตาราง return_reasons ไม่มี reason นี้
// หรือ reason ที่ใช้มีค่า is_returnable = 1 (สามารถคืนสต็อก)
// ข้อมูลก็จะไม่แสดงเป็นสินค้าชำรุด
```

---

## 📋 ขั้นตอนการแก้ไข

### ขั้นที่ 1: ตรวจสอบปัญหา

รันไฟล์ diagnostic tool:

```
http://localhost/diagnose_damaged_items.php
```

ไฟล์นี้จะตรวจสอบ:
- ✅ ว่า reason "สินค้าชำรุดบางส่วน" มีอยู่ในตาราง `return_reasons` หรือไม่
- ✅ ว่ามี `returned_items` ที่มี `is_returnable = 0` หรือไม่
- ✅ ว่า `po_id` ถูกบันทึกหรือไม่

### ขั้นที่ 2: เพิ่มเหตุผล "สินค้าชำรุดบางส่วน" ลงในฐานข้อมูล

ถ้า diagnostic tool บอกว่าไม่มี reason นี้ ให้รันไฟล์:

```
http://localhost/fix_damaged_partial_reason.php
```

ไฟล์นี้จะ:
1. ตรวจสอบว่า reason มีอยู่แล้วหรือไม่
2. หากไม่มี จะเพิ่ม reason ใหม่:
   - `reason_code`: `DMG-PARTIAL`
   - `reason_name`: `สินค้าชำรุดบางส่วน` ✅
   - `is_returnable`: `0` (ไม่สามารถคืนสต็อก)
   - `category`: `non-returnable`

### ขั้นที่ 3: ทดสอบระบบ

1. **เปิด browser console** (F12 -> Console tab)
2. **เปิด popup รับเข้าสินค้า** (receive_po_items.php)
3. **ดูข้อความใน console:**

   ```javascript
   // ควรเห็น:
   🔍 All return reasons fetched: {...}
   📋 Total reasons: 8
     - 1: จัดส่งไม่สำเร็จ (is_returnable: 1)
     - 2: ยกเลิกคำสั่งซื้อ (is_returnable: 1)
     - 3: ชำรุด/เสียหาย (is_returnable: 0)
     - ...
     - N: สินค้าชำรุดบางส่วน (is_returnable: 0) ✅
   ✅ Found damaged reason: reason_id = X
   ```

4. **ทดสอบการสร้าง return item:**
   - กดปุ่มชำรุด บน PO item
   - ควรแสดง Modal แทนที่จะแสดง "ไม่พบเหตุผล"

5. **ตรวจสอบโปเพ็ปบรรล:**
   - เมื่อสร้าง return item สำเร็จ
   - เปิด diagnostic tool อีกครั้ง
   - ควรเห็น `returned_items` ที่มี `is_returnable = 0` ออกมา

6. **ตรวจสอบ popup รับเข้าสินค้า:**
   - เปิด popup รับเข้าสินค้าสำหรับ PO นั้น
   - ดู console ของ browser:
     ```javascript
     🔍 loadDamagedUnsellableByPo called with poId: 123
     📥 API Response from get_damaged_unsellable_by_po: {...}
     ✅ Data received: [...]
     📋 displayDamagedUnsellableByPo called with items: [...]
     ✅ Found 1 damaged unsellable item(s)
     ✅ Damaged unsellable section displayed
     ```
   - **ส่วน "สินค้าชำรุดขายไม่ได้" ควรแสดงผลแล้ว** ✅

---

## 🔧 ไฟล์ที่ได้รับการปรับปรุง

### 1. `receive\receive_po_items.php`
- เพิ่มการบันทึก console log ใน `loadDamagedUnsellableByPo()`
- เพิ่มการบันทึก console log ใน `displayDamagedUnsellableByPo()`
- เพิ่มการบันทึก console log ใน `fetchDamagedReasonId()`
- เพิ่มการบันทึก `currentPoData` เพื่อตรวจสอบ poId ที่ส่งไป

### 2. `api\returned_items_api.php`
- เพิ่มการบันทึก debug log เมื่อสร้าง return item:
  ```
  📝 Created return record: return_id=X, po_id=Y, reason_name=Z, is_returnable=0
  ```

### 3. ไฟล์ใหม่ - Diagnostic Tools
- `fix_damaged_partial_reason.php` - เพิ่ม reason "สินค้าชำรุดบางส่วน" ลงในฐานข้อมูล
- `diagnose_damaged_items.php` - ตรวจสอบระบบ
- `api\test_damaged_items.php` - ทดสอบ API โดยตรง

---

## 📊 SQL Queries สำหรับตรวจสอบ

### ตรวจสอบ reason
```sql
SELECT * FROM return_reasons WHERE reason_name = 'สินค้าชำรุดบางส่วน';
```

### ตรวจสอบ returned_items ที่มี is_returnable = 0
```sql
SELECT 
    return_id, return_code, po_id, product_name, 
    reason_name, is_returnable, created_at
FROM returned_items
WHERE is_returnable = 0
ORDER BY created_at DESC;
```

### ทดสอบ API query
```sql
SELECT 
    ri.return_id, ri.return_code, ri.product_id, ri.product_name, ri.sku,
    ri.return_qty, ri.return_status, ri.is_returnable, ri.expiry_date, ri.created_at
FROM returned_items ri
WHERE ri.is_returnable = 0 AND ri.po_id = 123
ORDER BY ri.created_at DESC;
```

---

## 🎯 แนวทางการป้องกันในอนาคต

1. **ตอนติดตั้งระบบ:** ตรวจสอบว่า reason "สินค้าชำรุดบางส่วน" ถูกเพิ่มลงไปแล้ว
2. **ในไฟล์ setup:** เพิ่ม reason นี้เป็นค่าเริ่มต้น
3. **ดูแลระบบ:** ใช้ `diagnose_damaged_items.php` เป็นประจำเพื่อตรวจสอบสถานะ

---

## ✅ Checklist การแก้ไข

- [ ] รัน `diagnose_damaged_items.php` และตรวจสอบผลลัพธ์
- [ ] หากไม่มี reason "สินค้าชำรุดบางส่วน" ให้รัน `fix_damaged_partial_reason.php`
- [ ] ตรวจสอบ browser console ว่าแสดง "✅ Found damaged reason" หรือไม่
- [ ] ทดสอบการคลิกปุ่มชำรุดบน PO item
- [ ] สร้าง return item และตรวจสอบฐานข้อมูล
- [ ] เปิด popup รับเข้าสินค้าและตรวจสอบว่าแสดงส่วน "สินค้าชำรุดขายไม่ได้" หรือไม่
