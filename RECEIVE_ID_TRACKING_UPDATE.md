# 📦 เพิ่มการเก็บข้อมูล receive_id สำหรับติดตามลัตชุด/ชุดสินค้า

## 📝 สรุปการเปลี่ยนแปลง

ได้ทำการปรับปรุงระบบให้บันทึก `receive_id` จาก `issue_items` ในตาราง `returned_items` เพื่อช่วยติดตามลัตชุด (batch/lot) ของสินค้าที่ตีกลับมา พร้อมทั้งวันหมดอายุของสินค้า

---

## ✅ การปรับปรุงที่ทำ

### 1️⃣ เพิ่มคอลัมน์ `receive_id` ในตาราง `returned_items`
**ไฟล์:** `api/returned_items_api.php`

เพิ่มฟังก์ชัน `ensureReturnedItemsReceiveIdColumn()` เพื่อ:
- ตรวจสอบการมีอยู่ของคอลัมน์ `receive_id` ในตาราง `returned_items`
- เพิ่มคอลัมน์ `receive_id` หากไม่มี (ตำแหน่ง: หลังจาก `po_number`)
- สร้าง index สำหรับการค้นหาอย่างรวดเร็ว

```sql
ALTER TABLE returned_items 
ADD COLUMN receive_id INT NULL 
COMMENT 'receive_id from issue_items for batch/lot tracking' 
AFTER po_number;

ALTER TABLE returned_items 
ADD KEY idx_receive_id (receive_id);
```

---

### 2️⃣ เพิ่มคอลัมน์ `receive_id` ในตาราง `damaged_return_inspections`

อัปเดตการสร้างตาราง `damaged_return_inspections` เพื่อรวม `receive_id` ด้วย:

```sql
ALTER TABLE damaged_return_inspections 
ADD COLUMN receive_id INT NULL 
COMMENT 'receive_id from issue_items for batch/lot tracking' 
AFTER barcode;
```

---

### 3️⃣ ปรับปรุงการบันทึกเพื่อจับ receive_id จาก issue_items

**ไฟล์:** `api/returned_items_api.php`

#### สำหรับการตีกลับจาก Sales Order:

```php
// ดึงข้อมูล issue_items รวมถึง receive_id
$stmt = $pdo->prepare("
    SELECT ii.*, so.issue_tag 
    FROM issue_items ii
    JOIN sales_orders so ON ii.sale_order_id = so.sale_order_id
    WHERE ii.issue_id = :item_id AND ii.sale_order_id = :so_id
");

// จับ receive_id
$receive_id = isset($order_item['receive_id']) ? (int)$order_item['receive_id'] : null;
```

#### บันทึกลงในตาราง returned_items:

```php
INSERT INTO returned_items (
    ...
    po_number, receive_id, so_id, issue_tag, ...
    ...
    :po_number, :receive_id, :so_id, :issue_tag, ...
)
```

---

## 📊 โครงสร้างข้อมูล

### ตารางที่เกี่ยวข้อง:

| ตาราง | คอลัมน์ | ประเภท | รายละเอียด |
|-------|--------|---------|-----------|
| **issue_items** | receive_id | INT | *มีอยู่แล้ว* - ลัตชุดที่ออกสินค้า |
| **issue_items** | expiry_date | DATE | *เพิ่มใหม่* - วันหมดอายุ |
| **returned_items** | receive_id | INT | *เพิ่มใหม่* - บันทึกลัตชุด |
| **returned_items** | expiry_date | DATE | *มีอยู่แล้ว* - วันหมดอายุ |
| **damaged_return_inspections** | receive_id | INT | *เพิ่มใหม่* - ลัตชุดสำหรับตรวจสอบ |
| **damaged_return_inspections** | expiry_date | DATE | *มีอยู่แล้ว* - วันหมดอายุ |

---

## 🔄 ขั้นตอนการบันทึกข้อมูล

### การตีกลับจาก Sales Order:

```
issue_items (receive_id, expiry_date) 
    ↓
returned_items (receive_id, expiry_date) 
    ↓
damaged_return_inspections (receive_id, expiry_date) [ถ้าชำรุด]
```

### ข้อมูลที่เก็บไว้:

1. **receive_id**: ระบุว่าสินค้าออกมาจากลัตชุดไหน
   - ใช้สำหรับติดตามสินค้า
   - สามารถติดตามประวัติสินค้าของลัตชุดได้
   - ช่วยในการตรวจสอบคุณภาพของลัตชุด

2. **expiry_date**: วันหมดอายุของสินค้า
   - ติดตามสินค้าหมดอายุ
   - ช่วยในการวางแผนการใช้สินค้า

---

## 🧪 การทดสอบ

### สร้างไฟล์ทดสอบ:
- ไฟล์ `test_receive_id_capture.php`

### ตรวจสอบได้:
1. ตรวจสอบว่าคอลัมน์ `receive_id` มีอยู่ในตาราง `returned_items`
2. ตรวจสอบว่า `receive_id` ถูกบันทึกจาก `issue_items`
3. ตรวจสอบว่า `expiry_date` ถูกบันทึกอย่างถูกต้อง

### ขั้นตอนทดสอบ:
1. เปิดหน้า `returns/return_items.php`
2. ค้นหา Sales Order ที่ออกสินค้า
3. สร้างการตีกลับสินค้า
4. ตรวจสอบด้วย `test_receive_id_capture.php` ว่าข้อมูล `receive_id` ถูกบันทึก

---

## 📁 ไฟล์ที่แก้ไข

1. **api/returned_items_api.php**
   - เพิ่มฟังก์ชัน `ensureReturnedItemsReceiveIdColumn()`
   - อัปเดตตัวแปร `$receive_id` 
   - ปรับปรุง INSERT statement ให้รวม `receive_id`
   - อัปเดต damaged_return_inspections INSERT

2. **test_receive_id_capture.php** (สร้างใหม่)
   - ไฟล์ทดสอบสำหรับตรวจสอบการบันทึก `receive_id`

---

## 🎯 ผลลัพธ์

✅ ระบบจะบันทึก `receive_id` จาก `issue_items` ลงในตาราง `returned_items`

✅ สามารถติดตามลัตชุด/ชุดสินค้าที่ตีกลับมา

✅ เก็บข้อมูลวันหมดอายุพร้อมกับลัตชุด

✅ ข้อมูล `receive_id` และ `expiry_date` ปรากฏในทั้ง `returned_items` และ `damaged_return_inspections`

✅ สามารถใช้สำหรับสร้างรายงานและวิเคราะห์ข้อมูลการตีกลับตามลัตชุด

---

## 📌 หมายเหตุ

- ถ้าข้อมูลจาก `issue_items` ไม่มี `receive_id` จะบันทึกเป็น `NULL`
- `receive_id` มีประโยชน์สำหรับติดตามและจัดการสินค้าตีกลับตามลัตชุด
- สามารถเชื่อมโยงกับตาราง `receive_items` เพื่อดูข้อมูลเพิ่มเติมเกี่ยวกับลัตชุดได้
