# 📦 ระบบสินค้าตีกลับ (Return Items System)

## 📋 개요
ระบบจัดการสินค้าตีกลับสำหรับการบันทึกและติดตามสินค้าที่ลูกค้าตีกลับมาจากหลายเหตุผล

## 🎯 ลักษณะหลัก

### 1. การจำแนกการตีกลับ
ระบบแบ่งการตีกลับออกเป็น 2 ประเภท:

#### 🟢 **สามารถคืนสต็อก (Returnable)**
- ✅ จัดส่งไม่สำเร็จ
- ✅ ยกเลิกคำสั่งซื้อ
- ✅ ลูกค้าปฏิเสธรับสินค้า
- ✅ ส่งผิด

#### 🔴 **ไม่สามารถคืนสต็อก (Non-returnable)**
- ❌ ชำรุด/เสียหาย
- ❌ สินค้าปลอม
- ❌ อื่นๆ

## 📁 โครงสร้างไฟล์

### Setup & Database
```
setup_return_items_table.php          # Script สำหรับสร้างตารางฐานข้อมูล
```

### API Endpoints
```
api/returned_items_api.php             # API สำหรับจัดการสินค้าตีกลับ
```

### User Interface Pages
```
returns/return_items.php               # หน้าบันทึกสินค้าตีกลับ
returns/return_dashboard.php           # แดชบอร์ดการจัดการสินค้าตีกลับ
```

## 🗄️ ตารางฐานข้อมูล

### 1. `return_reasons` - เหตุผลการตีกลับ
| Column | Type | Description |
|--------|------|-------------|
| `reason_id` | INT | ID เหตุผล (Primary Key) |
| `reason_code` | VARCHAR(20) | รหัสเหตุผล (เช่น 001, 002...) |
| `reason_name` | VARCHAR(255) | ชื่อเหตุผล |
| `is_returnable` | TINYINT | 1=สามารถคืนสต็อก, 0=ไม่ได้ |
| `category` | VARCHAR(50) | returnable / non-returnable |
| `description` | TEXT | รายละเอียด |
| `is_active` | TINYINT | สถานะใช้งาน |
| `created_at` | TIMESTAMP | วันสร้าง |

### 2. `returned_items` - สินค้าตีกลับ
| Column | Type | Description |
|--------|------|-------------|
| `return_id` | INT | ID ตีกลับ (Primary Key) |
| `return_code` | VARCHAR(50) | เลขที่ตีกลับ (เช่น RET-2025-01-01-0001) |
| `po_id` | INT | ID ใบสั่งซื้อ (FK) |
| `po_number` | VARCHAR(50) | เลขที่ PO |
| `item_id` | INT | ID รายการสินค้า (FK) |
| `product_id` | INT | ID สินค้า (FK) |
| `product_name` | VARCHAR(255) | ชื่อสินค้า |
| `sku` | VARCHAR(50) | รหัสสินค้า |
| `barcode` | VARCHAR(100) | บาร์โค้ด |
| `original_qty` | DECIMAL(10,2) | จำนวนออกเดิม |
| `return_qty` | DECIMAL(10,2) | จำนวนตีกลับ |
| `reason_id` | INT | ID เหตุผล (FK) |
| `reason_name` | VARCHAR(255) | ชื่อเหตุผล |
| `is_returnable` | TINYINT | สามารถคืนสต็อกหรือไม่ |
| `return_status` | VARCHAR(50) | pending / approved / rejected / completed |
| `notes` | LONGTEXT | หมายเหตุเพิ่มเติม |
| `created_by` | INT | ผู้บันทึก (FK users) |
| `created_at` | TIMESTAMP | วันบันทึก |
| `approved_by` | INT | ผู้อนุมัติ (FK users) |
| `approved_at` | TIMESTAMP | วันอนุมัติ |
| `updated_at` | TIMESTAMP | วันแก้ไขล่าสุด |

## 🔌 API Endpoints

### 1. `get_reasons` - ดึงเหตุผลการตีกลับ
```
GET api/returned_items_api.php?action=get_reasons
```
**Response:**
```json
{
  "status": "success",
  "data": [
    {
      "reason_id": 1,
      "reason_code": "001",
      "reason_name": "จัดส่งไม่สำเร็จ",
      "is_returnable": 1,
      "category": "returnable",
      "description": "..."
    }
  ]
}
```

### 2. `search_po` - ค้นหา PO
```
GET api/returned_items_api.php?action=search_po&keyword=PO-2025-001
```
**Response:**
```json
{
  "status": "success",
  "data": [
    {
      "po_id": 1,
      "po_number": "PO-2025-001",
      "supplier_name": "Supplier Name",
      "created_at": "2025-01-15T10:30:00",
      "total_items": 5
    }
  ]
}
```

### 3. `get_po_items` - ดึงรายการสินค้าใน PO
```
GET api/returned_items_api.php?action=get_po_items&po_id=1
```
**Response:**
```json
{
  "status": "success",
  "data": [
    {
      "item_id": 1,
      "product_id": 1,
      "product_name": "Product Name",
      "sku": "SKU001",
      "receive_qty": 100,
      "returned_qty": 0,
      "available_qty": 100
    }
  ]
}
```

### 4. `create_return` - สร้างบันทึกตีกลับ
```
POST api/returned_items_api.php
```
**Request Body:**
```json
{
  "action": "create_return",
  "po_id": 1,
  "item_id": 1,
  "product_id": 1,
  "return_qty": 10,
  "reason_id": 1,
  "notes": "หมายเหตุเพิ่มเติม"
}
```

### 5. `get_returns` - ดึงรายการตีกลับ
```
GET api/returned_items_api.php?action=get_returns&status=all&is_returnable=all&limit=50&offset=0
```

### 6. `approve_return` - อนุมัติการตีกลับ
```
POST api/returned_items_api.php
```
**Request Body:**
```json
{
  "action": "approve_return",
  "return_id": 1
}
```

### 7. `reject_return` - ปฏิเสธการตีกลับ
```
POST api/returned_items_api.php
```
**Request Body:**
```json
{
  "action": "reject_return",
  "return_id": 1,
  "reason": "เหตุผลการปฏิเสธ"
}
```

## 🚀 วิธีการใช้งาน

### ขั้นตอนที่ 1: สร้างตารางฐานข้อมูล
1. เปิด `setup_return_items_table.php` ในเบราว์เซอร์
2. คลิกเพื่อสร้างตารางและเหตุผลเริ่มต้น

### ขั้นตอนที่ 2: บันทึกสินค้าตีกลับ
1. เข้าไปที่ `returns/return_items.php`
2. ค้นหา PO โดยใส่เลขที่ PO หรือเลขที่ออกสินค้า
3. เลือก PO ที่ต้องการ
4. เลือกสินค้าที่ต้องการตีกลับ
5. กรอกข้อมูล:
   - จำนวนที่ตีกลับ
   - เหตุผลการตีกลับ
   - หมายเหตุ (ถ้ามี)
6. คลิก "บันทึกสินค้าตีกลับ"

### ขั้นตอนที่ 3: จัดการการตีกลับ
1. เข้าไปที่ `returns/return_dashboard.php`
2. ดูแดชบอร์ดสำหรับสรุปข้อมูล
3. ค้นหาและเลือกการตีกลับที่ต้องการ
4. อนุมัติหรือปฏิเสธตามความเหมาะสม

## 📊 สถานะการตีกลับ

| สถานะ | คำอธิบาย |
|-------|---------|
| `pending` | รอการอนุมัติ |
| `approved` | อนุมัติแล้ว (อาจรอการคืนสต็อก) |
| `completed` | เสร็จสิ้น (คืนสต็อกเรียบร้อยแล้ว) |
| `rejected` | ปฏิเสธการตีกลับ |

## 🔐 สิทธิ์การเข้าถึง
- ✅ ผู้ใช้ที่ลงชื่อเข้าใช้ทั้งหมดสามารถบันทึกการตีกลับได้
- ✅ ผู้ดูแลระบบสามารถอนุมัติ/ปฏิเสธการตีกลับได้

## 🎨 ฟีเจอร์เพิ่มเติมในอนาคต
- [ ] ส่งออกรายงานเป็น Excel
- [ ] สร้างรายงานตามช่วงวันที่
- [ ] อินทิเกรตกับระบบจัดการสต็อก
- [ ] ส่งแจ้งเตือนเมื่อมีการตีกลับใหม่
- [ ] บาร์โค้ดสำหรับสินค้าตีกลับ
- [ ] ประเมินคุณภาพสินค้า

## 🐛 Troubleshooting

### ปัญหา: ไม่สามารถสร้างตารางได้
**วิธีแก้:**
1. ตรวจสอบการเชื่อมต่อฐานข้อมูล
2. ตรวจสอบสิทธิ์ของผู้ใช้ MySQL
3. ตรวจสอบชื่อฐานข้อมูลใน `config/db_connect.php`

### ปัญหา: ค้นหา PO ไม่พบผลลัพธ์
**วิธีแก้:**
1. ตรวจสอบตัวสะกดและหมายเลข PO
2. ตรวจสอบว่า PO มีสินค้าที่ได้รับแล้ว

## 📞 ติดต่อ & ความช่วยเหลือ
หากมีปัญหาหรือข้อเสนอแนะ โปรดติดต่อผู้ดูแลระบบ

---
**เวอร์ชั่น:** 1.0.0  
**วันสร้าง:** 2025-01-15  
**อัปเดตล่าสุด:** 2025-01-15
