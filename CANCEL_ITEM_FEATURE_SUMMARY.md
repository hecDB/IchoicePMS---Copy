# สรุปการเพิ่มฟังก์ชั่นยกเลิกสินค้า

## ✅ ที่เพิ่มเข้ามาแล้ว

### 1. UI Components (receive_po_items.php)

#### ปุ่มยกเลิกในตาราง
- เพิ่มปุ่ม "Clear" (เครื่องหมาย ✕) ถัดจากปุ่ม "Speed" (รับเข้าด่วน)
- ปุ่มมีสีแดง (btn-outline-danger) เพื่อบ่งบอกว่าเป็นการลบ/ยกเลิก
- ทำงานได้เฉพาะสินค้าที่ยังไม่ได้รับ (is_received == 0)

#### Modal ยกเลิกสินค้า (cancelItemModal)
- Header สีแดง (bg-danger) เพื่อเน้นความสำคัญ
- ฟิลด์:
  1. **สินค้า** - แสดงชื่อสินค้าที่เลือก
  2. **เหตุผลการยกเลิก** (required) - Dropdown 6 ตัวเลือก:
     - supplier_shortage - สินค้าไม่ครบตามจำนวนที่สั่ง
     - supplier_unavailable - ผู้จำหน่ายไม่มีสินค้า
     - product_discontinued - สินค้าถูกยกเลิก
     - wrong_order - ผิดข้อมูลสั่งซื้อ
     - customer_request - ตามคำขอของลูกค้า
     - other - อื่นๆ
  3. **หมายเหตุเพิ่มเติม** - Textarea สำหรับบอกรายละเอียด
- ปุ่มยืนยัน "ยืนยันการยกเลิก" (btn-danger)

### 2. JavaScript Functions (receive_po_items.php)

```javascript
// เปิด Modal ยกเลิก
showCancelItemModal(itemId, productName)

// จัดการการยกเลิก
saveCancelItem(itemId, reason, notes)

// Event Listener สำหรับปุ่ม Cancel
$('.cancel-item-btn').on('click')

// Event Listener สำหรับปุ่มยืนยัน
$('#confirmCancelItem').on('click')
```

### 3. Backend API (process_receive_po.php)

#### Action: `cancel_item`
**POST parameters:**
- `action` = 'cancel_item'
- `po_id` - รหัส PO
- `item_id` - รหัส Item
- `cancel_reason` - เหตุผลการยกเลิก
- `cancel_notes` - หมายเหตุเพิ่มเติม
- `po_number` - หมายเลข PO (สำหรับ logging)

**ทำการ:**
1. ตรวจสอบว่ารายการสินค้ามีอยู่
2. อัปเดต `is_cancelled = 1` ในตาราง purchase_order_items
3. บันทึก cancelled_by, cancelled_at, cancel_reason, cancel_notes
4. บันทึก log ไปยังตาราง activity_logs
5. ส่งกลับ JSON response

### 4. Database Schema Updates

#### ใหม่ที่เพิ่มในตาราง `purchase_order_items`:
```sql
- is_cancelled (TINYINT) - Default 0
- cancelled_by (INT) - Foreign Key to users
- cancelled_at (DATETIME) - เวลาการยกเลิก
- cancel_reason (VARCHAR 100) - เหตุผลการยกเลิก
- cancel_notes (TEXT) - หมายเหตุ
```

#### ตาราง `activity_logs` (สร้างใหม่):
```sql
- log_id (INT PRIMARY KEY AUTO_INCREMENT)
- user_id (INT FOREIGN KEY)
- action (VARCHAR 100)
- description (TEXT)
- created_at (DATETIME DEFAULT CURRENT_TIMESTAMP)
```

#### Automatic Initialization
- `process_receive_po.php` มีฟังก์ชั่น `initializeCancelSchema()`
- จะตรวจสอบและสร้างคอลัมน์/ตารางที่จำเป็นอัตโนมัติ
- ไม่จำเป็นต้องรัน SQL script ด้วยตนเอง

## 🔄 Flow การใช้งาน

1. **เปิด Modal รับเข้าสินค้า**
   - คลิกปุ่ม "รับเข้า" ในการ์ด PO

2. **เลือกสินค้าที่ยกเลิก**
   - คลิกปุ่ม "Clear" (✕) แดง

3. **กรอกข้อมูลในModal**
   - เลือก "เหตุผล" จาก dropdown
   - ระบุ "หมายเหตุ" (optional)

4. **ยืนยันการยกเลิก**
   - คลิกปุ่ม "ยืนยันการยกเลิก"
   - ระบบจะแสดง confirmation dialog

5. **บันทึกสำเร็จ**
   - Modal ปิด
   - ตาราง reload
   - หน้าจอ refresh เพื่ออัปเดตสถิติ

## 📊 Impact

- **รับเข้าสินค้า**: ยังสามารถรับส่วนที่มีได้ (receive some)
- **PO Status**: ยังถูกอัปเดตตามจำนวนที่รับได้
- **Cancelled Items**: ไม่นับรวมในสถิติการรับเข้า
- **Audit Trail**: บันทึกทั้งหมดในตาราง activity_logs

## ⚠️ สิ่งสำคัญ

✓ ยกเลิกไม่สามารถเปลี่ยนกลับได้
✓ บันทึกทั้งหมดด้วยชื่อผู้ใช้
✓ ต้องระบุเหตุผล (required field)
✓ สนับสนุนทั้งสินค้าปกติและสินค้าใหม่
